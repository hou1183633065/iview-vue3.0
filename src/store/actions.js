const actions = {
  addDataList (state, data) {
    console.log(state.dataList)
    // if (!state.addDataList.length) {
    //   state.dataList.push(data)
    // } else {
    //   state.addDataList.filter((item, index, self)=>{
    //     console.log(item, index, self)
    //   })
    // }
    // state.dataList.push(data)
  }
}

export default actions
